import type { App } from 'vue';
import { ECard, ESeparator, ECardGroup, EButton, ECalendar, EInput, ESpinner, EDropdown } from './index';
declare const install: (Vue: App) => void;
export { ECard, ECardGroup, ESeparator, EButton, ECalendar, EInput, ESpinner, EDropdown };
export default install;
